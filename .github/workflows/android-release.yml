# Release signed APK. Requires ANDROID_* secrets in repo Settings → Secrets → Actions.
name: Android APK release

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Dependencies
        run: |
          flutter --version
          flutter pub get

      - name: Configure Android release signing
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
        run: |
          set -e
          if [ -z "$KEYSTORE_BASE64" ]; then
            echo "::error::Missing ANDROID_KEYSTORE_BASE64. Repo → Settings → Secrets and variables → Actions."
            exit 1
          fi
          if [ -z "${STORE_PASSWORD:-}" ]; then
            echo "::error::Missing ANDROID_KEYSTORE_PASSWORD (keystore / store password)."
            exit 1
          fi
          if [ -z "${KEY_PASSWORD:-}" ]; then
            echo "::error::Missing ANDROID_KEY_PASSWORD (key password)."
            exit 1
          fi
          if [ -z "${KEY_ALIAS:-}" ]; then
            echo "::error::Missing ANDROID_KEY_ALIAS. Must match the alias inside your .jks (e.g. key0). Check android/key.properties locally for keyAlias=..."
            exit 1
          fi
          echo "$KEYSTORE_BASE64" | base64 -d > android/app/ci-release.jks
          printf '%s\n' \
            "storePassword=${STORE_PASSWORD}" \
            "keyPassword=${KEY_PASSWORD}" \
            "keyAlias=${KEY_ALIAS}" \
            "storeFile=ci-release.jks" \
            > android/key.properties

      - name: Build release APK
        run: flutter build apk --release

      - uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
